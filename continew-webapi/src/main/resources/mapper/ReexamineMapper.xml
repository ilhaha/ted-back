<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.continew.admin.certificate.mapper.ReexamineMapper">

    <select id="getcertificatesList" resultType="top.continew.admin.certificate.model.resp.ReexamineResp">
        SELECT tr.*, tct.certificate_name AS certificateName, su.nickname AS applicantName
        FROM ted_reexamine tr
                 LEFT JOIN ted_certificate_type tct ON tr.certificate_id = tct.id
                 LEFT JOIN sys_user su ON tr.applicant_id = su.id
           ${ew.getCustomSqlSegment}
    </select>

</mapper>